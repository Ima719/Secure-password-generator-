<!-- PWA Setup -->
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="theme-color" content="#4a6cf7">
<link rel="manifest" href="application/manifest+json,{
  %22name%22: %22Secure Password Generator%22,
  %22short_name%22: %22PassGen%22,
  %22start_url%22: %22.?utm_source=pwa%22,
  %22display%22: %22standalone%22,
  %22background_color%22: %22%23ffffff%22,
  %22theme_color%22: %22%234a6cf7%22,
  %22icons%22: [{
    %22src%22: %22https://cdn-icons-png.flaticon.com/512/633/633571.png%22,
    %22sizes%22: %22192x192%22,
    %22type%22: %22image/png%22
  }]
}">

<style>
body { font-family: Arial, padding: 20px; max-width: 500px; margin: 0 auto; }
input, button { width: 100%; padding: 12px; margin: 10px 0; box-sizing: border-box; }
#password { font-size: 18px; text-align: center; }
</style>

<h2>🔐 Secure Password Generator</h2>
<div id="password">اضغط لتوليد كلمة مرور</div>
<button onclick="generatePassword()">توليد كلمة مرور</button>
<button onclick="copyPassword()">نسخ</button>

<script>
function generatePassword() {
  const chars = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789!@#$%^&*";
  let pwd = "";
  for (let i = 0; i < 12; i++) {
    pwd += chars.charAt(Math.floor(Math.random() * chars.length));
  }
  document.getElementById("password").innerText = pwd;
}

function copyPassword() {
  const pwd = document.getElementById("password").innerText;
  if (pwd && pwd !== "اضغط لتوليد كلمة مرور") {
    navigator.clipboard.writeText(pwd).then(() => alert("تم النسخ!"));
  }
}
</script>

<!-- Service Worker بسيط -->
<script>
if ('serviceWorker' in navigator) {
  const sw = `
    self.addEventListener('fetch', () => {});
    self.addEventListener('install', () => self.skipWaiting());
    self.addEventListener('activate', e => e.waitUntil(clients.claim()));
  `;
  const blob = new Blob([sw], {type: 'application/javascript'});
  const url = URL.createObjectURL(blob);
  navigator.serviceWorker.register(url);
}
</script>
